# Pinned to the versions currently installed in .venv.
# Regenerate after changes with:  pip freeze > requirements.txt
Django==6.1.1
django-ninja==1.7.0
django-cors-headers==4.9.0
psycopg2-binary==2.9.12
dj-database-url==3.1.2
python-dotenv==1.2.3

# AI — Gemini (Google AI Studio) REST client, api/ai/gemini_client.py.
httpx==0.28.1
truststore==0.10.4

# Deploy
gunicorn==23.0.0 # gunicorn là 1 server production thay thế cho runserver
whitenoise==6.11.0   # whitenoise là thư viện giúp Django đơn giản hóa việc Deploy bằng cách tự phục vụ file tĩnh mà không cần thêm 1 server phụ như Nginx
