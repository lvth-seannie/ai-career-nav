# Base URL of the Django backend's API root (backend/core/urls.py mounts it
# at /api/). Defaults to http://localhost:8000/api when unset — see
# frontend/src/services/api.js.
VITE_API_URL=http://localhost:8000/api
